Add in dotFiles
  - awesomes
  - Add [y|n] in all operations

add Panel:
  - caps indicator
  - vlc genmon
  - workspace genmon

Put constant things in root directory and config (that changes) in a sub folder like `config/`
Add rofi config
Custom Icon pack
Add firefox config
Add Molokai Intellij Color Scheme

Add following color schems:
  - Google
  - Ayu
